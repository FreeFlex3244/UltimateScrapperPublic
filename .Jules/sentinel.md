## 2025-02-18 - SSRF Vulnerability in Scraper
**Vulnerability:** The recursive web scraper accepted user-supplied URLs without validation and crawled internal networks (127.0.0.1, private IPs).
**Learning:** Web scrapers inherently trust user input as "start URLs". Without strict validation, they become open proxies to internal services.
**Prevention:** Always validate destination IPs against private/loopback ranges before initiating requests. Use `ipaddress` module for robust checks.
## 2025-02-18 - SSRF Vulnerability via Open Redirects in Scraper\n**Vulnerability:** The scraper was vulnerable to SSRF via open redirects because  was following redirects blindly to internal IP ranges, bypassing the initial URL validation.\n**Learning:** Just validating the starting URL is insufficient. Scrapers must handle HTTP redirects manually and validate every new  URL against private/loopback IP address ranges to prevent SSRF.\n**Prevention:** Disable automatic redirects with  in HTTP clients. Manually follow redirects, track the redirect count to avoid infinite loops, and run  validation on every extracted  header before initiating the next request.
## 2025-02-18 - SSRF Vulnerability via Open Redirects in Scraper
**Vulnerability:** The scraper was vulnerable to SSRF via open redirects because requests.get was following redirects blindly to internal IP ranges, bypassing the initial URL validation.
**Learning:** Just validating the starting URL is insufficient. Scrapers must handle HTTP redirects manually and validate every new Location URL against private/loopback IP address ranges to prevent SSRF.
**Prevention:** Disable automatic redirects with allow_redirects=False in HTTP clients. Manually follow redirects, track the redirect count to avoid infinite loops, and run is_safe_url validation on every extracted Location header before initiating the next request.
